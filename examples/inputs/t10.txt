alpha = x * x + 2 * x * y + y * y ;
beta = x * x - 2 * x * y + y * y ;
gamma = (x + y) * (x - y) ;
delta = alpha - gamma * beta + (alpha + beta) * gamma ;
result = delta * (alpha + 1) - beta * (gamma - 1) + alpha * beta * gamma ;
