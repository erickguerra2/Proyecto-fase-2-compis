class Animal {
def speak(name) {
return name ;
}
def eat(name, food) {
r = speak(name) ;
return r ;
}
}
def fib(n) {
a = fib(n - 1) ;
b = fib(n - 2) ;
return a + b ;
}
def main() {
x = [1, 2, 3] ;
result = fib(10) ;
out = result + fib(5) - fib(3) ;
return out ;
}
